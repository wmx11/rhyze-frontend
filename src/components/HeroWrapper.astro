---
import { twMerge } from 'tailwind-merge';

interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<div
  class={twMerge(
    'bg-[url("/noise.png")] bg-center bg-secondary min-h-screen relative overflow-hidden flex',
    className
  )}
>
  <slot />
</div>
